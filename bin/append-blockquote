#! /usr/bin/env node

const stdin = require('fs').readFileSync('/dev/stdin', 'utf8')

// 先頭に改行を付加
let text = "\n" + stdin
// 改行を指定文字に置換
text = text.replace(/[\n]/g, "\n> ")
// 先頭の改行を削除
text = text.replace(/^.*?\n/, '')

// console.log(stdin.replace(/[\n]/g, "\n> "))
console.log(text)
